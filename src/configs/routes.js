// import INITIALIZERS from "./initializers";

// const { BASE_URL } = INITIALIZERS;

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  HOME: () => "/",
  USERS: () => `/users`,
  NOT_FOUND: () => `/404`,
};
